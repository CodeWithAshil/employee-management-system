# Copy this file to `.env` and fill in your real values.
# `.env` is git-ignored so secrets never get committed.
#
# NOTE: Spring Boot does not auto-load .env files. Either export these in your
# shell before running, or set them in your IDE run configuration.

DB_USERNAME=root
DB_PASSWORD=Pass@123
